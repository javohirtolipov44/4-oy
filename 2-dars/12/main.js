import { createDiv, createParagraph, createButton } from "./elements.js";

const div = createDiv("div", "div elementi");
document.body.appendChild(div);

const paragraph = createParagraph("paragraph", "p elementi");
document.body.appendChild(paragraph);

const button = createButton("button", "button tugmasi");
document.body.appendChild(button);
